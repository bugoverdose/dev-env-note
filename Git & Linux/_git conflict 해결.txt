바로 직전 pull에서 conflict 발생한 경우

git merge --abort // merge 취소.
충돌 해결 
git pull origin main // 다시 pull 받기.

==================================================
PR 브랜치에서 마스터를 merge받아 conflict 발생시키기
해결하여 커밋, git push하고, 다시 PR을 마스터에 머지

cf) 그냥 마스터로 merge시켜서 해결해도 된다고 하기는 함.

==================================================