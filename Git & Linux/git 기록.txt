git 원격 저장소 초기화

> git init            // git 기록 시작 

==============================================================
git 원격 저장소 초기화
 
> rm -rf ./.git       // 로컬에 존재하는 .git 디렉토리를 삭제한다. 

==============================================================
cf) git의 staging area에서만 제거. 내 컴퓨터에는 그대로 유지. VCS 수작업이 더 편리.

> git rm -r 폴더명/ 

==============================================================